'$schema': 'https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json'

# This will likely need updating since keeping it as working
# in both EX and LB might be tricky/impossible.

name: Columns
# replaces: radicati_drupal_theme:columns
status: stable
group: Layout
slots:
  columns:
    title: Columns
    description: Add column components here.
    allowedComponents:
      - radicati_drupal_theme:column