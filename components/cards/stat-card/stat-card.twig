{% set classes = [
  'card',
  'stat-card'
] %}
{% if content_attributes is not defined %}
  {% set content_attributes = create_attribute() %}
{% endif %}
<div {{ content_attributes.addClass(classes) }}>
  {% if block('media') is defined and block('media')|trim is not empty %}
    <div class="stat-card__media slot slot--media">
      {% block media %}{% endblock %}
    </div>
  {% endif %}

  <div class="stat-card__value">{{ stat_value }}</div>
  <div class="stat-card__label">{{ stat_label }}</div>
</div>